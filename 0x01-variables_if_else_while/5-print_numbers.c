#include <stdio.h>

/**
 * main - Entry point
 *
 * Desc: Prints numbers 0 to 9
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	return (0);
}
